# 生产容器通过同源 Nginx 代理访问 Gateway；开发服务器使用 Vite 代理。
VITE_API_BASE_URL=/api/v1

# 令牌发送域名白名单（逗号分隔的完整 origin 列表）。
# 同源请求始终允许携带令牌；跨源请求只有在此白名单中的域名才会携带 Authorization 头。
# 留空时仅允许同源请求携带令牌。
VITE_TOKEN_ALLOWED_ORIGINS=
